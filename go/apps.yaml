hacklab_acl: # all values are hex-encoded, wrap them in quotes to make sure they are interpreted as strings
    aid: "031337" # This must be unique
    uid_read_key_id: "02"
    acl_read_key_id: "03"
    acl_write_key_id: "04"
